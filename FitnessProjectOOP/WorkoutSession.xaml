<Window x:Class="FitnessProjectOOP.WorkoutSessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitnessProjectOOP"
        mc:Ignorable="d"
        Title="WorkoutSession" Height="500" Width="600" WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="txtTemplateName" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="{Binding Date, StringFormat='{}{0:MMMM dd, yyyy HH:mm}'}" Foreground="Gray"/>
        </StackPanel>

        <!-- Exercises -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="exerciseList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <CheckBox IsChecked="{Binding IsCompleted}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding ExerciseName}" FontWeight="Bold"/>
                            <TextBlock Text=" — " />
                            <TextBlock Text="{Binding Sets}" />
                            <TextBlock Text=" x " />
                            <TextBlock Text="{Binding Reps}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Duration (min):" Width="120"/>
                <TextBox x:Name="txtDuration" Width="100"/>
            </StackPanel>

            <StackPanel Margin="0,10,0,0">
                <TextBlock Text="Notes:"/>
                <TextBox x:Name="txtNotes" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
            </StackPanel>

            <Button Content="Finish &amp; Save" 
            HorizontalAlignment="Right" 
            Width="120" 
            Margin="0,15,0,0" 
            Click="FinishAndSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
